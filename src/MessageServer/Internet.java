package MessageServer;

public class Internet extends InternetClient {
	
	HashSet<Hostname> host = new HashSet<Hostname>();
	
	public void registerStatus(Hostname host, NetworkClient clientTo); 
	host.openConnection(NetwortkClient clientTo); 
	host.closeConnection(NetworkClient hostTo); 
	
}
