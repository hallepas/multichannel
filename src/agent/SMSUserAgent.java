package agent;
import java.util.*;

class SMSUserAgent {
	SMSUserAgent smsUserAgent = new SMSUserAgent();
	Account sms = new Account();
	
	smsUserAgent.sendMessage(Account sms); //sms wird versendet
}
