package agent;
import java.util.*;

 class MMSUserAgent extends UserAgent {
		MMSUserAgent mmsUserAgent = new MMSUserAgent();
		Account mms = new Account();
		
		mmsUserAgent.sendMessage(Account mms); //mms wird versendet
}
